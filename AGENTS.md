Use the GitHub CLI API with tokens from `C:\Users\avrca\Documents\Projects\hosts.yml`.

Never run any Kluster checks in this repository.
Do not call `kluster_code_review_auto`, `kluster_code_review_manual`, or `kluster_dependency_check` under any circumstances.
